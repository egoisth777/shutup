# Agent instructions

Read [CLAUDE.md](CLAUDE.md) before non-trivial work. Preserve repository shape, runtime invariants, safe-write policy, and measured-claims rule.

Use tests as behavior contracts. Keep changes within assigned ownership. Do not modify another worker's files during parallel work; record requests in `notes/worker-c-requests.md`.
